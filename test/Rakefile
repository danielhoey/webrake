require_relative '../src/webrake'

task :default => ['build']

Rules.new(Rake.application, FileSystem.new, {
  '*.html.erb' => Filters::PassThroughFilter.new
})

#puts Rake.application.tasks.inspect
task :build_clean => ['clean_intermediate_files', 'build']
